To run the unit tests, use the following:

    phpunit --configuration phpunit.xml

You can also run tests for just a group:

    phpunit --configuration phpunit.xml --group MyProject_Example

By default, coverage reports and TestDox are generated in the logs/
subdirectory.
